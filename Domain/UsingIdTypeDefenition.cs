global using IdType = System.Guid;

/*

В качестве идентификаторов используется UUID 
для возможности создавать сразу несколько предметов в памяти
и сохранения их одним вызовом SaveChanges

Используется версия ULID
имеет возрастающие по времени генерации значение и соотвественно имеет последовательную запись в БД







*/